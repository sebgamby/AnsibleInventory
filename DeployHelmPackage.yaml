---
- name: "Deploym Helm package"
  hosts: localhost
  tasks:

     - name: Add chart repo
       kubernetes.core.helm_repository:
          name: proget
          repo_url: "http://192.158.1.4/helm/demo"

     - name: Install demo Chart
       kubernetes.core.helm:
        name: nginx-demo
        namespace: default
        chart_ref: proget/demo