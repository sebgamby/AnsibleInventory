---
- name: "Install a Docker container"
  hosts: docker
  tasks:
    - name: "Create and start a container"
      ansible.windows.win_powershell:
        script: |
          docker container create --name WinDockerDemo -p 80:80 gambyseb/private:win-docker-demo-0.2
          docker container start WinDockerDemo
